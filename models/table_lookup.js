define([], function() {
  return () => {};
});

function create_table_lookup (inputs, outputs) {
  console.log('create table model', arguments);
  setTimeout(() => {window.location.reload();}, 15000);

  const model = {};


}
